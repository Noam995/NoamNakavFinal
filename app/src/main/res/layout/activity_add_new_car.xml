<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F7FA">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- כותרת -->
        <TextView
            android:text="הוספת רכב חדש"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="#1F3A5F"
            android:layout_marginBottom="16dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <!-- מספר רישוי -->
        <EditText
            android:id="@+id/etLicense"
            android:hint="מספר רישוי"
            android:background="@drawable/bg_input"
            android:padding="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- מותג -->
        <Spinner
            android:id="@+id/spBrand"
            android:entries="@array/carBrandsArr"
            android:background="@drawable/bg_input"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- דגם -->
        <EditText
            android:id="@+id/etModel"
            android:hint="דגם"
            android:background="@drawable/bg_input"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- צבע ושנה -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <Spinner
                android:id="@+id/spColor"
                android:entries="@array/colorArr"
                android:background="@drawable/bg_input"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content"/>

            <Spinner
                android:id="@+id/spYear"
                android:entries="@array/yearArr"
                android:background="@drawable/bg_input"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content"/>
        </LinearLayout>

        <!-- מחיר -->
        <EditText
            android:id="@+id/etPrice"
            android:hint="מחיר (₪)"
            android:inputType="number"
            android:background="@drawable/bg_input"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- קילומטראז -->
        <EditText
            android:id="@+id/etKm"
            android:hint="קילומטראז"
            android:inputType="number"
            android:background="@drawable/bg_input"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- יד -->
        <Spinner
            android:id="@+id/spHand"
            android:entries="@array/hands"
            android:background="@drawable/bg_input"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- תיבת הילוכים -->
        <RadioGroup
            android:id="@+id/radiogroupgear"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <RadioButton
                android:id="@+id/rBmanual"
                android:text="ידני"
                android:textColor="#1F3A5F"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

            <RadioButton
                android:id="@+id/rBautmatic"
                android:text="אוטומטי"
                android:textColor="#1F3A5F"
                android:layout_marginStart="16dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
        </RadioGroup>

        <!-- בעלות ודלק -->
        <Spinner
            android:id="@+id/spOwnership"
            android:entries="@array/previousOwnerTypeArr"
            android:background="@drawable/bg_input"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <Spinner
            android:id="@+id/spGas"
            android:entries="@array/fuelTypeArr"
            android:background="@drawable/bg_input"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- תאריך טסט -->
        <EditText
            android:id="@+id/etDatetilTest"
            android:hint="תוקף טסט"
            android:inputType="date"
            android:background="@drawable/bg_input"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- מנוע -->
        <EditText
            android:id="@+id/etEnginevolume"
            android:hint="נפח מנוע"
            android:inputType="number"
            android:background="@drawable/bg_input"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <EditText
            android:id="@+id/etEngine"
            android:hint="סוג מנוע"
            android:background="@drawable/bg_input"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- תמונה -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_marginTop="12dp"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:scaleType="centerCrop"
            android:background="#DDE3EA"/>

        <!-- כפתורי תמונה -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <Button
                android:id="@+id/btncamara"
                android:text="📷 מצלמה"
                android:backgroundTint="#3A6EA5"
                android:textColor="#FFFFFF"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content"/>

            <Button
                android:id="@+id/btnimagebrowse"
                android:text="🖼 גלריה"
                android:backgroundTint="#3A6EA5"
                android:textColor="#FFFFFF"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:layout_width="0dp"
                android:layout_height="wrap_content"/>
        </LinearLayout>

        <!-- כפתור הוספה -->
        <Button
            android:id="@+id/btnAdd"
            android:text="הוסף רכב"
            android:textSize="18sp"
            android:textStyle="bold"
            android:backgroundTint="#1F3A5F"
            android:textColor="#FFFFFF"
            android:layout_marginTop="16dp"
            android:layout_width="match_parent"
            android:layout_height="56dp"/>

    </LinearLayout>
</ScrollView>
